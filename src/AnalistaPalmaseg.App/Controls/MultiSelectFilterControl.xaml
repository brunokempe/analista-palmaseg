<UserControl x:Class="AnalistaPalmaseg.App.Controls.MultiSelectFilterControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             x:Name="Root"
             FontFamily="Lato, Segoe UI">

    <Grid>
        <ToggleButton x:Name="Toggle"
                      Style="{x:Null}"
                      Height="40"
                      Width="Auto"
                      Padding="10,0"
                      HorizontalAlignment="Stretch"
                      HorizontalContentAlignment="Stretch"
                      VerticalContentAlignment="Center"
                      Cursor="Hand">
            <ToggleButton.Template>
                <ControlTemplate TargetType="ToggleButton">
                    <Border x:Name="Bd"
                            Background="White"
                            BorderBrush="#B0B0B0"
                            BorderThickness="1"
                            CornerRadius="4">
                        <ContentPresenter Margin="{TemplateBinding Padding}"
                                          HorizontalAlignment="Stretch"
                                          VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource PrimaryBrush}" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="BorderBrush" Value="#6B7280" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </ToggleButton.Template>

            <!-- Content fica fora do ControlTemplate para que os bindings ElementName=Root
                 (que apontam para este UserControl) consigam resolver — dentro de um
                 ControlTemplate eles ficariam em outro escopo de nomes e não resolveriam. -->
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0"
                           Text="{Binding ResumoTexto, ElementName=Root}"
                           FontSize="12"
                           VerticalAlignment="Center"
                           TextTrimming="CharacterEllipsis"
                           Foreground="#4B5563" />
                <materialDesign:PackIcon Grid.Column="1" Kind="ChevronDown"
                                         Width="16" Height="16"
                                         VerticalAlignment="Center"
                                         Foreground="#6B7280" Margin="6,0,0,0" />
            </Grid>
        </ToggleButton>

        <Popup x:Name="FilterPopup"
               IsOpen="{Binding IsChecked, ElementName=Toggle, Mode=TwoWay}"
               PlacementTarget="{Binding ElementName=Toggle}"
               Placement="Bottom"
               StaysOpen="False"
               AllowsTransparency="True"
               PopupAnimation="Fade"
               Opened="FilterPopup_Opened">
            <Border Background="White"
                    BorderBrush="#D1D5DB" BorderThickness="1"
                    CornerRadius="4"
                    MinWidth="{Binding ActualWidth, ElementName=Toggle}"
                    MaxHeight="280"
                    Margin="0,2,0,0">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.2" />
                </Border.Effect>
                <DockPanel>
                    <Border DockPanel.Dock="Bottom"
                            BorderBrush="#E5E7EB" BorderThickness="0,1,0,0"
                            Padding="8,6">
                        <TextBlock Text="Limpar seleção"
                                   FontSize="11" FontWeight="SemiBold"
                                   Foreground="{StaticResource PrimaryBrush}"
                                   Cursor="Hand"
                                   MouseLeftButtonUp="LimparSelecao_Click" />
                    </Border>
                    <ScrollViewer VerticalScrollBarVisibility="Auto" MaxHeight="220">
                        <ItemsControl ItemsSource="{Binding Opcoes, ElementName=Root}" Margin="4">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <CheckBox Content="{Binding Valor}"
                                              IsChecked="{Binding Selecionado, Mode=TwoWay}"
                                              FontSize="12" Padding="6,4"
                                              Margin="2" />
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </DockPanel>
            </Border>
        </Popup>
    </Grid>
</UserControl>
