<UserControl x:Class="AnalistaPalmaseg.App.Views.RetencaoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
             Background="#F8FAFC">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Styles/Colors.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <DockPanel Margin="24,20,24,24">

        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,16">
            <TextBlock Text="Produtor:" VerticalAlignment="Center"
                       FontSize="13" Foreground="#64748B" Margin="0,0,12,0" />
            <ComboBox ItemsSource="{Binding Produtores}"
                      SelectedItem="{Binding ProdutorSelecionado}"
                      Width="200"
                      Style="{StaticResource MaterialDesignOutlinedComboBox}" />
        </StackPanel>

        <materialDesign:Card DockPanel.Dock="Bottom" Padding="16">
            <StackPanel>
                <TextBlock Text="Evolução da taxa de retenção por período" FontSize="13" FontWeight="Medium"
                           Foreground="#0F172A" Margin="0,0,0,12" />
                <lvc:CartesianChart Series="{Binding Series}"
                                    XAxes="{Binding AxesX}"
                                    Height="360" LegendPosition="Bottom" />
                <StackPanel Orientation="Horizontal" Margin="0,12,0,0">
                    <Border Background="#DCFCE7" CornerRadius="4" Padding="8,4" Margin="0,0,8,0">
                        <TextBlock Text="≥ 90%  Boa retenção" FontSize="12" Foreground="#15803D" />
                    </Border>
                    <Border Background="#FEF9C3" CornerRadius="4" Padding="8,4" Margin="0,0,8,0">
                        <TextBlock Text="80–89%  Atenção" FontSize="12" Foreground="#854D0E" />
                    </Border>
                    <Border Background="#FEE2E2" CornerRadius="4" Padding="8,4">
                        <TextBlock Text="&lt; 80%  Crítico" FontSize="12" Foreground="#991B1B" />
                    </Border>
                </StackPanel>
            </StackPanel>
        </materialDesign:Card>

    </DockPanel>
</UserControl>
